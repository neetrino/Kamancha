/**
 * Below-fold home content streams via `page.tsx` Suspense.
 * Hero chrome is in `layout.tsx` so entrance motion is not replayed here.
 */
export default function HomeLoading() {
  return null;
}
