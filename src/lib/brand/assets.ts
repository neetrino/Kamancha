import { staticAssetUrl } from "@/lib/media/static-asset-url";

/** Shared brand decorative ornament (PNG — keep quality; one asset everywhere). */
export const BRAND_ORNAMENT_SRC = staticAssetUrl("/assets/brand/ornament.png");

/** Mobile home hero plate — Figma 181:476 (765×765, rotated 180°). */
export const HOME_HERO_PLATE_SRC = staticAssetUrl(
  "/assets/brand/home/hero-plate.webp",
);

/** Mobile home category carousel dish (plate cutout, no table backdrop). */
export const HOME_MOBILE_CATEGORY_DISH_SRC = staticAssetUrl(
  "/assets/brand/home/mobile-category-dish.webp",
);

/** Contact page flanking hands — Figma 244:509 / 251:510. */
export const CONTACT_HAND_PARCHMENT_SRC = staticAssetUrl(
  "/assets/brand/contact/hand-parchment.webp",
);
export const CONTACT_HAND_QUILL_SRC = staticAssetUrl(
  "/assets/brand/contact/hand-quill.webp",
);
